[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

[dev]
  targetPort = 8080

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
